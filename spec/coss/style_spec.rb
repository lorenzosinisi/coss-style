require "spec_helper"

describe Coss::Style do
  it "has a version number" do
    expect(Coss::Style::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
